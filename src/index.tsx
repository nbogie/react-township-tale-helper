import React from 'react';
import ReactDOM from 'react-dom';
import TownshipVoiceInterface from './components/TownshipVoiceInterface';

//fonts
import "@fontsource/abel"
import "@fontsource/merriweather"


ReactDOM.render(
  <React.StrictMode>
    <TownshipVoiceInterface />
  </React.StrictMode>,
  document.getElementById('root')
);
